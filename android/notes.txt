sdkmanager "platform-tools" "emulator" "platforms;android-34" "system-images;android-34;google_apis;x86_64"


avdmanager create avd -n Pixel_6_Pro_API_34_PlayStore_ARM -k "system-images;android-34;google_apis;x86_64" --device "pixel_7_pro"


~/Library/Android/sdk/emulator/emulator -avd pixel_7_pro

avdmanager list avd

sdkmanager --list
sdkmanager --list_installed
avdmanager list device "pixel_6_pro"

 usr/local/var/lib/android-sdk

 avdmanager create avd -h

 emulator -avd Pixel_6_Pro_API_34_PlayStore_ARM -gpu on -skin 1440x3120

 sdkmanager --uninstall  "system-images;android-34;google_apis_playstore;arm64-v8a"


 watchman watch-del-all && rm -rf $TMPDIR/react-*  && rm -fr $TMPDIR/metro-cache* && rm -fr $TMPDIR/haste-map-metro*  &&  rm -rf ios/build && xcrun simctl erase all && rm -rf node_modules/ && npm cache verify && npm install && react-native run-ios -- --reset-cache

npx react-native init App01 --version 0.71.7

for i in $( gem list --local --no-version | grep cocoapods );
do 
    sudo gem uninstall $i; 
done

lsof -i :8081

emulator -webcam-list -avd Pixel_6_Pro_API_34_PlayStore_ARM  -camera-back webcam0

emulator @pixel_7_pro -partition-size 2048

npx react-native-clean-project

~/.android/avd

./gradlew assembleDebug

react-native run-android  --mode=release

./gradlew clean
rm -rf .gradle

unzip -lv test.apk | grep "\.so"

 unzip -lv android/app/build/outputs/apk/debug/app-debug.apk | grep lib*.so

adb logcat
