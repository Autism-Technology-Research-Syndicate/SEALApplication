import { StyleSheet } from 'react-native';

const stylesheet = StyleSheet.create({
    container: {
        height: '100%',
        width: '100%',
        paddingBottom: 10
    },
});
export default stylesheet;