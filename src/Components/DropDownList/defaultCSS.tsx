import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  dropDown: {
    marginBottom: 15,
    borderColor: '#305070',
  },
  labels: {
    fontSize: 15,
  }
});

export default styles;

