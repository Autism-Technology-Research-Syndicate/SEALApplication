/**
 * @format
 */

import {setupDatabase} from './bootstrapping/initializeDatabase';
import React, {useEffect} from 'react';
import {AppRegistry, StyleSheet, Text, View} from 'react-native';

setupDatabase();
